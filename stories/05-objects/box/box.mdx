# Box

Box component offer a visual container for any content inside.

```html
<div class="box">
  <p>Some content</p>
</div>
```

## Bordered variant

Instead of a shadow, the box container can use a border instead with the `box--bordered` variant.

```html
<div class="box box--bordered">
  <p>Some content</p>
</div>
```
